// /groups/messages — the private messages, expanded into a page of their own.
// Drawn by the group shell and the one conversation window (see
// components/DirectMessagesHost and lib/dmWindow); this exists so the address
// resolves on a reload or a shared link.
export default function DirectMessagesPage() {
  return null;
}
