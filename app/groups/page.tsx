// /groups — the list of your groups. Drawn by the group shell, which picks the
// view from the address itself (see components/groups/GroupAppShell and
// lib/groupNavigation), so moving in and out of a group never waits on the
// server for this page. It exists so the address resolves.
export default function GroupsPage() {
  return null;
}
